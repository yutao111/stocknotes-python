# StockNotes 提醒系统规范

本文件是三日低吸、横盘整理企稳、日内反弹及其后续回测功能的单一规则文档。

修改任何提醒逻辑、参数、通知、样本、回测或相关数据库结构前，必须先阅读本文件。修改完成后必须同步更新本文件，并运行完整测试。

文档状态标记：

- **已实现**：当前代码和数据库已经具备，修改时必须保持或显式迁移。
- **待实现**：已经确定的目标设计，不能当作当前线上行为。
- **建议**：后续优化方向，实施前仍需用真实数据验证。

## 1. 目标与边界

提醒系统只做三件事：

1. 识别客观行情结构。
2. 保存触发时的完整条件。
3. 用触发后的真实行情评估提醒是否有效。

提醒不是自动交易指令，也不保证上涨。任何历史回测结果都不能被解释为未来收益保证。

两套策略必须独立统计：

- 三日低吸使用日线数据，重点评估 T+1、T+3、T+5、T+10。
- 日内反弹使用分钟数据，重点评估信号后 5、10、20 分钟和当日收盘。

## 2. 数据来源

### 2.1 日线行情

表：`daily_prices`

主要字段：`stock_code`、`trade_date`、`open`、`high`、`low`、`close`、`volume`、`amount`、`source`、`fetched_at`。

`daily_prices.volume` 的数据库单位固定为“股”，不随 `source` 改变。K 线 JSON 对外输出“手”，并携带 `volume_unit: "lot"`。

数据库版本 `15` 将旧 `tencent-realtime`、`akshare-eastmoney` 日线成交量从“手”迁移为“股”；版本 `17` 使用标准化日线修复仍可追溯的三日低吸样本 candle 和事件量比；版本 `18` 对成交额反推确认仍为“手”的腾讯历史日线转为“股”；版本 `19` 回退此前被错误放大 100 倍、且成交额反推确认原本为“股”的腾讯实时日线。版本 `18` 和 `19` 均会再次修复可追溯样本和事件量比。缺少对应日线的旧样本不猜测、不伪造单位或量比。

用途：

- 三日低吸识别。
- 三日低吸后续收益。
- 日内反弹的昨收基准。
- 历史 K 线和交易复盘。

### 2.2 分钟行情

表：`intraday_quotes`

主要字段：`stock_code`、`trade_date`、`quote_minute`、`price`、`previous_close`、`volume`、`amount`、`fetched_at`。

`intraday_quotes.volume` 是腾讯行情截至该分钟的当日累计成交量，单位固定为“手”，不是该分钟独立成交量。日内反弹只在同一交易会话内对相邻累计值做差分。

要求：

- 使用行情源交易所时间归档，不用本地接收时间作为行情时间。
- `previous_close` 是日内反弹的硬基准，不能缺失。
- 分钟数据不足时可以做单日回放，但不能宣称长期胜率或据此过度优化参数。

### 2.3 重点观察池

表：`watchlist_stocks`

只有重点观察池里的标的产生自动策略提醒。持仓、成交单导入和历史行情同步不会绕过重点观察池限制。

## 3. 交易时间与同步

后台自动行情同步周期为 `5` 秒。

持仓和重点观察页面每 `5` 秒通过短 HTTP 请求读取后台同步完成后的用户隔离快照，顶部通知中心每 `20` 秒读取一次。请求只读取已经写入 SQLite 的行情和通知，不额外访问外部行情源；不用 SSE 长连接，避免多个页面长期占满浏览器对本地 HTTP/1.1 服务的并发连接。旧 `/api/realtime-stream` 地址返回 `204`，使仍打开的旧页面停止 EventSource 自动重连。

自动同步时间：

- 交易日 `09:15–11:30`。
- 交易日 `13:00–15:00`。
- 周末不运行。

日内反弹的有效连续竞价时间更严格：

- 上午 `09:30–11:30`。
- 下午 `13:00–15:00`。

`09:15–09:29` 的集合竞价数据不能作为日内反弹低点确认。上午和下午必须分开计算，不能跨 `11:30–13:00` 午休拼接结构。

## 4. 三日低吸

状态：基础识别、通知、研究样本池、真实通知事件和日线后续收益为**已实现**；参数组合优化为**待实现**。

策略代码：`THREE_DAY_DIP`

核心函数：

- `three_day_dip_params()`。
- `evaluate_three_day_dip()`。
- `evaluate_realtime_alerts()`。

### 4.1 基础形态

默认使用前置下跌日、基准日和信号日组成的日线窗口。规则可要求前置日收阴、收盘价逐日走低、信号日处于前期低点附近，并从日内低点修复。

当前默认要求基准日严格收红：

```text
baseline.close > baseline.open
```

平盘 `close == open` 不算红。该条件由 `require_bullish_baseline` 控制，默认启用，并进入三种形态共用的前置条件。标准顺序为：基准日收红，随后两个交易日连续收阴且收盘逐日走低，第三天作为信号日判断止跌修复。

### 4.2 形态分类

#### 衰竭止跌型

- 连续下跌后实体较小。
- 低点后有一定修复。
- 涨跌幅不宜过大。
- 量能不能明显放大。
- 表示卖压衰竭，不等于强势反转。

#### 下影止跌型

- 出现明显下影线。
- 下影线相对实体足够长。
- 低点后收回一定幅度。
- 默认只作为盘中候选，不能直接当作确认买点。

#### 强修复型

- 前置累计跌幅较大。
- 信号日从低点明显修复。
- 收回当日振幅的较大部分。
- 信号日涨幅达到要求。
- 三种形态中强度最高。

### 4.3 通知阶段

`CANDIDATE` 表示盘中候选，允许暂不强制完整收盘量能。

`CONFIRMED` 表示收盘确认，必须使用完整信号日行情和成交量；下影止跌型默认不升级为确认。

同一用户、标的、交易日和阶段最多一条自动通知。去重键包含：

```text
user_id:alert_type:stock_code:trade_date:stage
```

## 4A. 横盘整理企稳

状态：独立提醒类型、收盘确认、历史测试、研究样本池、真实通知事件和日线后续收益为**已实现**。

策略代码：`CONSOLIDATION_STABILIZATION`

规则版本：`consolidation-stabilization-v1`

核心函数：

- `consolidation_stabilization_params()`。
- `evaluate_consolidation_stabilization()`。
- `evaluate_consolidation_stabilization_alerts()`。

### 4A.1 计算窗口

默认使用横盘前 `10` 个交易日、横盘期 `3` 个交易日和确认日，共 `14` 根日 K。横盘前最后 `4` 日作为背景探底窗口，背景区间振幅不得超过 `10%`，且必须出现低于最后横盘区最低价的低点。确认日只能使用当日收盘后完整的开高低收和成交量，不产生盘中候选。

### 4A.2 横盘与确认

横盘期区间定义为：

```text
range_high = max(consolidation.high)
range_low = min(consolidation.low)
range_ratio = range_high / range_low - 1
```

默认要求：

- 背景窗口存在低于最后横盘区最低价的探底低点。
- 背景窗口振幅不超过 `10%`。
- 横盘区间振幅不超过 `4%`。
- 确认日收盘价严格高于横盘期最高价。
- 确认日成交量至少为横盘期平均成交量的 `1.2` 倍。

`daily_prices.volume` 统一保存“股”。东方财富历史接口返回“手”，写入时乘以 `100`。腾讯实时和腾讯历史接口对不同标的都可能返回“手”或“股”：仅当 `成交额 / 收盘价 / 成交量` 位于 `75–125` 时判定为“手”并乘以 `100`，其余保留原值，避免误改 ETF 等无法可靠由收盘价反推成交股数的记录。新浪历史接口直接写入。K 线 API 输出时统一除以 `100` 转成“手”。`intraday_quotes.volume` 单独保存腾讯当日累计原始单位，只用于同源分钟差分和量比。

探底回收不是硬条件，普通窄幅横盘也可识别。趋势分类只生成标签，不阻止提醒。

### 4A.3 趋势标签

以横盘前趋势窗口首尾收盘涨跌分类，默认阈值为 `5%`：

- `DOWN_STABILIZATION`：前置涨跌不高于 `-5%`，显示“下跌企稳”。
- `UP_CONTINUATION`：前置涨跌不低于 `+5%`，显示“上涨中继”。
- `NEUTRAL_BREAKOUT`：其余情况，显示“中性横盘突破”。

康龙化成 `300759` 的基准样本为 `2026-06-15` 至 `2026-06-17` 横盘，`2026-06-18` 收盘放量突破，分类为 `DOWN_STABILIZATION`。

### 4A.4 通知与收益

只生成 `CONFIRMED` 通知。同一用户、股票和交易日最多一条：

```text
user_id:CONSOLIDATION_STABILIZATION:stock_code:trade_date:CONFIRMED
```

成功生成通知后写入 `alert_signal_events`，保存实际触发价、规则版本、完整参数快照，以及整理区上下沿、区间振幅、突破量比和前置趋势涨跌。复用 `alert_signal_event_outcomes` 计算 T+1、T+3、T+5、T+10 收盘收益、最高收益和最大回撤。

## 5. 日内反弹

状态：水下过滤、会话隔离、两级通知、会话状态、真实通知事件和分钟/日线后续收益为**已实现**；结构有效期和参数组合优化为**待实现**。

策略代码：`INTRADAY_REBOUND`

核心函数：

- `intraday_rebound_params()`。
- `intraday_session()`。
- `evaluate_intraday_rebound()`。
- `evaluate_intraday_rebound_alerts()`。

### 5.1 硬前置条件：相对昨收水下

低点必须满足：

```text
low <= previous_close * (1 - min_waterline_drop_ratio)
```

当前默认 `min_waterline_drop_ratio = 0.02`，即低点相对昨收下跌至少 `2%`。

这是日内低吸的硬过滤条件。不满足时不产生早期观察，也不产生结构确认。

### 5.2 辅助急跌条件

同时要求局部高点到低点的跌幅达到 `min_drop_ratio`，默认 `2%`。它与水下条件不同：

- 水下条件比较低点和昨收。
- 局部急跌条件比较盘中局部高点和低点。

### 5.3 会话隔离

上午 `AM` 为 `09:30–11:30`，下午 `PM` 为 `13:00–15:00`。

每个会话独立寻找：

- 低点。
- 第一次反弹高点。
- 更高低点。
- 固定突破位。
- 早期观察时间。
- 结构确认时间。

状态表：`intraday_rebound_states`。

状态唯一维度：

```text
user_id, alert_type_id, stock_code, trade_date, session
```

### 5.4 低点确认

默认低点出现后至少等待 `3` 分钟，期间不能继续创新低。最低价本身不是买点，必须等待价格行为确认。

### 5.5 早期观察

默认条件：

- 水下至少 `2%`。
- 局部急跌至少 `2%`。
- 低点后至少等待 `3` 分钟。
- 期间不再创新低。
- 从低点反弹至少 `0.6%`。
- 早期量比至少 `1.0`。
- 从低点反弹不能超过 `2%`。

通知阶段为 `CANDIDATE`，标题使用“日内反弹·早期观察”。

含义：可以开始关注或小仓试错，不代表结构完全确认。

### 5.6 第一次反弹高点

低点之后第一次满足以下条件的局部高点被锁定：

- 从低点反弹至少 `0.4%`。
- 价格不低于前一分钟。
- 下一分钟价格回落。

一旦锁定，`breakout_price` 不得随着后续上涨继续抬高。这样可以防止通知在上涨一大段后才出现。

### 5.7 更高低点与结构确认

第一次反弹高点之后，回踩最低价必须高于原始低点。若回踩跌破原低点，本轮结构失效并重新寻找新结构。

结构确认默认要求：

- 已锁定第一次反弹高点。
- 已形成更高低点。
- 当前价格突破固定突破位。
- 突破腿量比至少 `1.2`。
- 从低点反弹不超过 `2%`。

通知阶段为 `CONFIRMED`，标题使用“日内反弹·结构确认”。

### 5.8 失效与重新武装

目标规则如下，但目前只有会话隔离、新低切换和反弹上限已经实现；“结构有效期”和“第一次突破后禁止旧位二次确认”仍为**待实现**。

以下情况最终不得继续使用旧结构确认：

- 价格跌破原始低点。
- 突破后长期不延续，结构已过期。
- 已上涨一大段、回落后再次碰到旧突破位。
- 下午重新碰到上午突破位。

特别规则（**待实现**）：第一次突破后如果已经拉升很多，再回落后重新突破旧突破位，属于“过期结构二次突破”，不能再发低吸确认。必须形成新的急跌、新低和新反弹结构。

日内反弹去重键必须包含：

```text
user_id:alert_type:stock_code:trade_date:session:trough_minute:stage
```

## 5A. 日常提醒配置

状态：**已实现**。

日常提醒设置是全局统一配置，保存于各自 `alert_types` 记录中，对所有本地用户生效；提醒通知仍只发给将该股票放入重点观察池的用户。后台管理的“提醒类型 → 日常提醒”可设置：

- 涨幅提醒开关及阈值，默认 `4%`。
- 跌幅提醒开关及阈值，默认 `4%`。
- 涨停提醒开关。
- 跌停提醒开关。

四种提醒独立评估和去重。例如跌停同时达到跌幅阈值时，会分别生成跌停提醒和跌幅提醒。

## 5B. 涨幅提醒

状态：**已实现**。

策略代码：`WATCHLIST_OPEN_GAIN`

仅检查重点观察池中的股票。实时行情当前价相对昨收的当日涨幅达到或超过配置阈值时生成一条 `CONFIRMED` 通知，默认阈值为 `4%`：

```text
current_price / previous_close - 1 >= 0.04
```

昨收或当前价缺失、无效时不提醒。同一用户、股票和交易日最多提醒一次：

```text
user_id:WATCHLIST_OPEN_GAIN:stock_code:trade_date
```

通知保存昨收、当前价、当日涨幅和交易日。该提醒独立于日内反弹开关，并与重点观察涨停提醒共同归入通知页面的“日常提醒”。

### 5C. 跌幅提醒

状态：**已实现**。

策略代码：`WATCHLIST_OPEN_LOSS`

仅检查重点观察池中的股票。实时行情当前价相对昨收的当日跌幅达到或超过配置阈值时生成一条 `CONFIRMED` 通知，默认阈值为 `4%`：

```text
1 - current_price / previous_close >= 0.04
```

昨收或当前价缺失、无效时不提醒。同一用户、股票和交易日最多提醒一次：

```text
user_id:WATCHLIST_OPEN_LOSS:stock_code:trade_date
```

通知保存昨收、当前价、当日跌幅和交易日。该提醒独立于其他策略开关，并归入通知页面的“日常提醒”。

### 5D. 涨停与跌停提醒

策略代码：`WATCHLIST_LIMIT_UP`、`WATCHLIST_LIMIT_DOWN`

仅检查重点观察池中的股票。实时行情源必须返回有效的涨停价或跌停价；当前价达到对应价格时，生成一条 `CONFIRMED` 通知。不会按照固定涨跌幅推算涨停或跌停。

同一用户、股票和交易日每个类型最多提醒一次：

```text
user_id:WATCHLIST_LIMIT_UP:stock_code:trade_date
user_id:WATCHLIST_LIMIT_DOWN:stock_code:trade_date
```

### 5E. 日常提醒页面范围

“日常提醒”筛选只展示本地自然日当天创建的 `WATCHLIST_LIMIT_UP`、`WATCHLIST_LIMIT_DOWN`、`WATCHLIST_OPEN_GAIN` 和 `WATCHLIST_OPEN_LOSS` 通知，时间范围为当天 `00:00`（含）至次日 `00:00`（不含）。历史通知记录和其他提醒类型不删除，也不受该筛选影响。

## 6. 参数基线

当前生产默认基线：

```json
{
  "lookback_minutes": 120,
  "min_drop_ratio": 0.02,
  "min_waterline_drop_ratio": 0.02,
  "candidate_min_rebound_ratio": 0.006,
  "candidate_min_volume_multiple": 1.0,
  "confirmation_min_volume_multiple": 1.2,
  "min_trough_age_minutes": 3,
  "first_bounce_min_ratio": 0.004,
  "max_entry_rebound_ratio": 0.02,
  "enabled": true
}
```

代码默认值、管理页面和 `stocknotes.db` 中 `alert_types.params_json` 必须保持一致。修改参数后必须核对数据库中的实际生效值。

横盘整理企稳默认基线：

```json
{
  "consolidation_days": 3,
  "setup_lookback_days": 4,
  "setup_max_range_ratio": 0.10,
  "max_range_ratio": 0.04,
  "trend_lookback_days": 10,
  "trend_threshold_ratio": 0.05,
  "min_breakout_volume_ratio": 1.2,
  "enabled": true
}
```

## 7. 提醒样本与回测

提醒不能只写入 `notifications`。通知是展示层数据，不能作为唯一回测数据源。

真实自动通知统一写入：

- `alert_signal_events`：通知事件、阶段、触发价、规则版本、参数快照和指标快照。
- `alert_signal_event_outcomes`：分钟、会话收盘和 T+N 后续收益。

只有成功插入的真实通知才生成事件；重复轮询和未发送的匹配不进入胜率统计。实时行情同步刷新分钟收益，历史日线同步刷新 T+N 收益。

数据库升级会幂等回填仍保留在 `notifications` 中的历史三日低吸和日内反弹通知。回填事件优先使用通知详情或对应分钟行情恢复触发价，并在参数快照中写入 `backfilled: true`；无法可靠恢复触发价的通知跳过，不伪造收益。

三日低吸回填按同一用户、标的、交易日和阶段复用已有实时事件，不因实时行情时间与通知时间相差几秒而重复生成统计事件。

### 7.1 三日低吸样本

现有表：`alert_signal_samples`。

自动样本和手工样本必须通过 `source` 区分：

- `AUTO`：真实自动提醒产生。
- `MANUAL`：人工加入或研究样本。

自动样本至少保存：

- 用户、标的、信号日期。
- 形态类型。
- 触发阶段。
- 信号 K 线。
- 规则指标。
- 规则版本。
- 生成时间。

真实候选和确认还会分别写入 `alert_signal_events`，不受研究样本表“同一股票同一天一个样本”的约束。

### 7.2 三日低吸后续收益

现有表：`alert_signal_outcomes`。

以信号日收盘价为主要基准，计算：

- T+1、T+3、T+5、T+10 收盘收益。
- 未来 1/3/5/10 日最高收益。
- 未来 1/3/5/10 日最大回撤。

数据不足时使用 `NULL`，不能把不完整窗口当作完整结果。必须保存计算截止日期。

### 7.3 横盘整理企稳样本

状态：**已实现**，复用 `alert_signal_samples` 和 `alert_signal_outcomes`，但必须按 `alert_type_id` 与三日低吸样本隔离展示、扫描和审核。

手工加入和历史扫描分别保存：

- 用户、标的、信号日期和来源。
- 趋势标签：`DOWN_STABILIZATION`、`UP_CONTINUATION` 或 `NEUTRAL_BREAKOUT`。
- 完整趋势窗口、横盘期和确认日 K 线。
- 整理区上下沿、区间振幅、突破量比和前置趋势涨跌。
- 规则版本、审核状态和备注。

手工样本默认直接确认；历史扫描样本默认待确认。两类样本都以确认日收盘价为基准，复用 T+1、T+3、T+5、T+10 收盘收益、最高收益和最大回撤计算。

### 7.4 日内反弹样本

状态：**已实现**，复用统一表 `alert_signal_events`，不再单独创建策略专属样本表。

每个早期观察和结构确认分别保存：

- 用户、标的、交易日、会话。
- 低点时间和价格。
- 昨收和水下幅度。
- 第一次反弹高点和固定突破位。
- 更高低点。
- 早期观察时间和价格。
- 结构确认时间和价格。
- 早期量比和突破量比。
- 参数快照和规则版本。
- 生成时间。

### 7.5 日内反弹后续收益

状态：**已实现**，写入 `alert_signal_event_outcomes`。

早期观察和结构确认必须分开计算，以各自触发价格为基准：

- 信号后 5 分钟收益。
- 信号后 10 分钟收益。
- 信号后 20 分钟收益。
- 当日收盘收益。
- 当日最高收益。
- 当日最大回撤。
- 是否先跌破失效位。
- 是否达到 `+1%`、`+2%` 目标。

当前直接存储 5/10/20 分钟、会话收盘、会话最高收益、会话最大回撤以及 T+1/T+3/T+5/T+10；目标价先后顺序和失效位先后顺序仍为后续增强项。

## 8. 结果分类

### 8.1 三日低吸

建议分类：

- `VALID`：后续收益为正且回撤可控。
- `STRONG`：T+1、T+3、T+5 多数为正。
- `FAILED`：信号后持续下跌。
- `SPIKE_RETRACE`：先上涨后回落。
- `WEAK`：收益接近 0，盈亏比不足。
- `PENDING`：后续交易日数据不足。

### 8.2 日内反弹

建议分类：

- `VALID`：信号后 10/20 分钟或收盘收益为正。
- `EARLY_ONLY`：早期观察有效，但结构确认没有成立。
- `FAILED`：信号后先跌破失效位。
- `LATE`：通知时已经消耗大部分反弹空间。
- `SPIKE_RETRACE`：曾快速上涨但随后回落。
- `PENDING`：分钟数据不足。

## 9. 参数优化

参数优化必须基于真实触发样本及后续收益，不能只看触发数量或单日案例。

### 9.1 统计指标

每组参数至少统计：

- 信号数量。
- 胜率。
- 平均收益和中位收益。
- T+1/T+3/T+5/T+10 收益。
- 平均最大回撤及回撤分位数。
- 盈亏比。
- 达到目标收益的比例。
- 早期观察与结构确认差异。
- 上午与下午差异。
- 不同股票和不同形态的稳定性。

### 9.2 优化目标

优先级：

1. 降低错误和过晚通知。
2. 提高风险调整后收益。
3. 保持足够信号数量。
4. 避免为了某一天或某只股票过拟合。

可参考：

```text
期望收益 = 胜率 × 平均盈利 - 失败率 × 平均亏损
风险调整收益 = 平均收益 / 平均最大回撤
```

### 9.3 防止未来数据泄漏和过拟合

- 评估某分钟时只能使用该分钟及之前的数据。
- 不能用收盘结果反推盘中是否应该触发。
- 参数搜索、验证和最终测试必须按时间切分。
- 不能删除失败信号后再计算胜率。
- 不能只用江化微、半导体设备 ETF 或任意单一标的调参。

建议至少积累：

- 30 个交易日：初步观察。
- 60 个交易日：参数比较。
- 120 个交易日：相对稳定验证。

## 10. 页面要求

### 10.1 三日低吸页面

真实通知数量、T+1/T+3/T+5/T+10 平均收益、胜率和最近事件为**已实现**。以下细分和优化视图仍为**待实现**：

- 自动样本数量和手工样本数量。
- T+1/T+3/T+5/T+10 平均收益。
- 各形态胜率。
- 候选与确认对比。
- 待评估数量。
- 失败信号列表。
- 参数回测入口。

### 10.2 日内反弹页面

真实通知数量、5/10/20 分钟与会话收盘平均收益、胜率和最近事件为**已实现**。以下细分和优化视图仍为**待实现**：

- 早期观察和结构确认数量。
- 5/10/20 分钟平均收益。
- 当日收盘收益。
- 平均最大回撤。
- 上午和下午对比。
- 过晚信号列表。
- 水下幅度分组结果。

### 10.3 通知详情

每条通知应能查看：

- 触发条件。
- 参数快照。
- 低点、突破位和失效位。
- 入场参考价。
- 后续收益。
- 最大回撤。
- 最终结果标签。

## 11. 数据清理规则

删除通知展示记录不等于删除回测样本。

清理通知时：

- 明确说明只删除 `notifications` 还是同时删除样本和状态。
- 默认保留样本、参数快照和后续收益。
- 如果用户明确要求删除历史提醒，应记录删除范围。
- 不得因为通知已读或已删除而失去回测数据。

## 12. 规则版本

三日低吸使用：

```text
THREE_DAY_DIP_RULE_VERSION
```

当前版本：`three-day-dip-v7`。v7 新增“基准日必须严格收红”的默认条件；历史旧版本事件和样本保留原版本，不重写。

日内反弹应使用独立版本常量：

```text
INTRADAY_REBOUND_RULE_VERSION
```

状态：三类策略提醒均已将独立规则版本写入统一事件表。

横盘整理企稳使用独立版本常量 `CONSOLIDATION_STABILIZATION_RULE_VERSION`，当前版本为 `consolidation-stabilization-v1`。

规则版本必须写入：

- 信号样本。
- 通知详情。
- 回测结果。
- 参数组合结果。

改变触发条件、通知阶段、去重逻辑、失效逻辑或收益口径时，必须升级对应版本。

## 13. 修改提醒功能前的强制清单

修改前必须回答：

1. 修改的是三日低吸、横盘整理企稳还是日内反弹？
2. 使用的是日线还是分钟线？
3. 是否改变触发条件？
4. 是否改变通知阶段？
5. 是否改变去重逻辑？
6. 是否改变失效或重新武装逻辑？
7. 是否需要保存新的参数快照？
8. 是否需要数据库迁移？
9. 是否增加未来数据泄漏风险？
10. 是否需要更新管理页面？
11. 是否需要更新后续收益计算？
12. 是否需要更新本文件？

## 14. 修改后的验收要求

至少运行：

```bash
python3 -m py_compile app.py test_app.py
python3 -m unittest -v
```

提醒功能修改还必须覆盖：

- 规则纯函数测试。
- 通知去重测试。
- 用户隔离测试。
- 水下 2% 测试。
- 上午/下午会话隔离测试。
- 新低重新武装测试。
- 历史分钟回放测试。
- 后续收益计算测试。
- 数据库迁移测试。

## 15. 标准闭环

```text
行情采集
→ 规则判断
→ 发送通知
→ 保存完整信号样本
→ 等待后续行情
→ 计算真实收益和回撤
→ 分类有效/失败信号
→ 参数回测
→ 时间外验证
→ 再修改生产参数
```

以后不能只问“有没有提醒”，必须同时回答：

- 提醒时价格是多少？
- 之后涨了多少？
- 最大回撤是多少？
- 哪种形态有效？
- 哪个参数组合更稳定？
